# Base URL of the FastAPI backend (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000

# Google Maps JavaScript API key (Maps JS + Places + Directions enabled).
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
